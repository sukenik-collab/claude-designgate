# Capability Map — [Screen Name]
#
# Generated by DesignGate during Step 1.5 (Capability Reconciliation).
# Held in working context during the session. Optionally saved to
# docs/capability_maps/[screen-name].md for reference.
#
# Source model:
#   Intent source:        spec    — what we want the system to be
#   Reality declaration:  bundle  — what the caller says currently exists
#   Code:                         — optional diagnostic lens only
#
# The bundle is complete as declared. Code cannot add to this map.

---

## Metadata

- **Screen:** [screen name]
- **User role:** [from bundle]
- **Spec section:** [reference]
- **Bundle provided:** yes

---

## User Capabilities

Must be represented somewhere in the new design. Existence is not a design question —
HOW each is expressed is decided in the Step 2 audit.

Sourced from caller declaration in the screen context bundle. This list is closed —
code files cannot add to it.

| Capability | Note |
|------------|------|
| [verb phrase] | — |
| [verb phrase] | — |

---

## System Behaviors

Context only. These may change entirely in the new design and do not constrain it.
Listed here so the design team is aware of current behavior if relevant.

| Behavior | Note |
|----------|------|
| [behavior] | Context only |

---

## Data Entities

Schema must be preserved. Interaction logic need not be.

For each entity: enumerate every field with its type, nullable status, and UI dependency.
If a field is absent from the current schema but required by a declared User Capability,
include it and mark it `MISSING — [prerequisite]`. Missing fields are as important as
present ones — they surface UI dependencies that cannot be met yet.

**[EntityName]**
| Field | Type | Nullable | UI dependency |
|-------|------|----------|---------------|
| id | UUID | no | — |
| [field] | [type] | yes / no | [UI element that depends on this, or —] |
| [field] | [type] | yes / no | MISSING — [what must exist before the dependent capability works] |

<!-- Repeat entity block for each declared entity. -->

---

## Not Carried Forward

Capabilities excluded by caller declaration or identified as non-user-facing.

| Item | Reason |
|------|--------|
| [item] | Caller-excluded: [reason] |

---

## Spec Coverage Check

Capabilities in the bundle that are NOT explicitly addressed in the spec.
These are surfaced to the user for confirmation before the audit begins.

| Capability | Status |
|------------|--------|
| [capability] | Not in spec — confirm carry-forward |
